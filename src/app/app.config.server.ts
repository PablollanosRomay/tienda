// Server config disabled for browser build. This project targets
// Angular 15 (NgModule-based). Restore SSR config only when
// upgrading Angular and setting up a dedicated server build.

export {};
