// Server-side rendering entry removed from browser build to avoid
// compilation errors when the project is built for the browser.
// If you need SSR, restore the original file and adjust package
// dependencies and tsconfig to compile server-side code separately.

export {};
